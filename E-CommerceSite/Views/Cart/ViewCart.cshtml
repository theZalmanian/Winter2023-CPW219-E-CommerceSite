@model IEnumerable<ProductCartViewModel>

@{
    ViewData["Title"] = "Shopping Cart";
}

<h1>Shopping Cart</h1>

<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ProductName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ProductPrice)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model) {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.ProductName)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.ProductPrice)
                    </td>
                    <td>
                        <a class="btn btn-secondary" asp-controller="Product" asp-action="Details" asp-route-productID="@item.ProductID">Details</a>
                        <a class="btn btn-danger" asp-action="Remove" asp-route-productID="@item.ProductID">Remove</a>
                    </td>
                </tr>
        }
    </tbody>
</table>
